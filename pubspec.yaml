name: resource_id
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  build: ^1.2.0
  build_config: ^0.4.1
  path: ^1.7.0

dev_dependencies:
  build_resolvers: ^1.3.11

flutter:
